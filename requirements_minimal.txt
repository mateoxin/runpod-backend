# 🚀 Minimal requirements for instant startup
# Heavy dependencies installed at runtime for faster deployment
# Based on successful runpod-fastbackend/ approach

runpod>=1.7.0
pyyaml>=6.0
python-dotenv>=1.0.0
requests>=2.31.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
httpx>=0.25.0
python-multipart>=0.0.6

# Lightweight utilities
aiofiles>=23.2.1
psutil>=5.9.6
boto3>=1.34.0

# Runtime installations for optimal deployment speed (installed at runtime):
# torch, transformers, diffusers, accelerate, huggingface_hub[cli]